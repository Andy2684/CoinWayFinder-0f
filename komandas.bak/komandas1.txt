🔁 Хочешь, чтобы это запускалось не всегда?
Можно временно отключить hook командой:

bash
Copy
Edit
chmod -x .git/hooks/pre-push
А чтобы снова включить:

bash
Copy
Edit
chmod +x .git/hooks/pre-push
